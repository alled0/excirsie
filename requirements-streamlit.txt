# Taharrak — Streamlit web version dependencies
# Install with:  pip install -r requirements-streamlit.txt
#
# Uses opencv-python-headless (no display needed in a server / cloud environment).
# pyttsx3 is intentionally omitted — TTS is disabled in the web version.

streamlit>=1.28.0
streamlit-webrtc>=0.47.0
av>=10.0.0
mediapipe>=0.10.0
opencv-python-headless>=4.8.0
numpy>=1.24.0
